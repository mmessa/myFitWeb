<p id="notice"><%= notice %></p>

<h1>Listing Your Updates</h1>

<table class="table">
  <thead>
    <tr>
      <th>Date Added</th>
      <th>Weight</th>
      <th>Notes</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @updates.each do |update| %>
      <tr>
          <td><%= update.created_at %></td>
          <td><%= update.weight %></td>
          <td><%= update.notes %></td>
          <td><%= link_to 'Show', update %></td>
          <td><%= link_to 'Edit', edit_update_path(update) %></td>
          <td><%= link_to 'Destroy', update, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Update', new_profile_update_path(@profile) %>

<h1> Listing Your Workouts </h1>

<% render 'workouts/index' %>
